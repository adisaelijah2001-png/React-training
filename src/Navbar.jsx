const Navbar = () => {
    return (
        <nav className="navbar">
            <h1>The new project</h1>
            <div className="links">
                <a href="#">Home</a>
                <a href="#">About</a>
                <a href="#">Blog</a>
            </div>
        </nav>
    );
}

export default Navbar;
